<?xml version="1.0" encoding="UTF-8"?>
<format>
  <instructions>
    <instruction>Provide all code changes as a unified diff (patch) format.</instruction>
    <instruction>Include all modifications for all files in a single ```diff code block.</instruction>
    <instruction>Do not provide explanations outside the diff code block.</instruction>
  </instructions>
  <examples>
    <example>```diff</example>
    <example>--- a/path/to/original/file.js</example>
    <example>+++ b/path/to/original/file.js</example>
    <example>@@ -10,7 +10,7 @@</example>
    <example>-old line</example>
    <example>+new line</example>
    <example>```</example>
  </examples>
</format>
